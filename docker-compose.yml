services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gridwise_dev
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/__pycache__
    command: sh -c "exec uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
